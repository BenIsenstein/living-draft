# Pin one shadcn component base

Living Draft mirrors a deliberately pinned shadcn Base UI and Base Nova source set with near-zero divergence rather than maintaining Base UI, Radix, React Aria, or Web Component variants in parallel. One lineage makes Artifact code predictable and directly transferable, while commit, CLI, dependency, and file-hash provenance makes upstream changes reviewable instead of silently following latest.
