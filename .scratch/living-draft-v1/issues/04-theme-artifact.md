# 04: Preview, apply, and save artifact themes

**What to build:** A Studio user can experiment with a complete shadcn theme without writing files, apply it as the active project baseline, and save it as the artifact's handoff snapshot.

**Blocked by:** 03: Supervise external artifact sessions

**Status:** ready-for-agent

- [ ] Preview scopes validated theme variables to the active artifact without a disk write.
- [ ] Apply atomically updates the generated active-theme baseline.
- [ ] Save validates and writes the complete theme and font snapshot to artifact metadata.
- [ ] Base Nova neutral and warm-yoga presets are available.
